apiVersion: v1
kind: ConfigMap

metadata:
  name: app-config
  namespace: dev

data:
  DATABASE_URL: "mysql://rullabcd:rullabcd@mysql:3306/coba?connection_limit=10&pool_timeout=20&connect_timeout=60"
  MYSQL_DATABASE: "coba"
  NEXTAUTH_URL: "http://demo.rullabcd.com"
  NODE_ENV: "production"
