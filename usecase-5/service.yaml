apiVersion: v1
kind: Service

metadata:
  name: webapp-service
  namespace: webapp

spec:
  selector:
    app: webapp
    tier: frontend

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  - protocol: TCP
    port: 6379
    targetPort: 6379
